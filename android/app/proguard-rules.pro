# Flutter's own classes are kept by the Flutter Gradle plugin's default rules.
# Nothing extra is needed today; this file exists so release minification has a
# place to record any keep rules a future platform channel requires.
